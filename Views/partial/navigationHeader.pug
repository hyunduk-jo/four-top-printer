header.navigation-header__container
  .navigation-header__logo-container
    a(href=routes.home)
      h1 LOGO
    .navigation-header__brand-name
      span 4TOP
      span ALL 4th TO PEOPLE
  .navigation-header__menus
    ul
      a(href=`${routes.nav}${routes.company}`)
        li 회사소개
      a(href=`${routes.nav}${routes.gallery}`)
        li 3D 갤러리
      a(href=`${routes.nav}${routes.community}`)
        li 커뮤니티
      a(href=`${routes.nav}${routes.rental}`)
        li 렌탈
      a(href=`${routes.nav}${routes.promotion}`)
        li 프로모션
      a(href=`${routes.nav}${routes.magazine}`)
        li 매거진
      a(href=`${routes.nav}${routes.serviceCenter}`)
        li 고객센터
  .search
    form(action="search", method="get")
        input(type="text", placeholder="")
        i.xi-search
